FILE(
    GLOB 
    VENDOR_FOLDERS
    *
)

FOREACH(VENDOR_FOLDER ${VENDOR_FOLDERS})
    IF(IS_DIRECTORY ${VENDOR_FOLDER})
        ADD_SUBDIRECTORY(${VENDOR_FOLDER})
        MESSAGE(STATUS "Added vendor: ${VENDOR_FOLDER}")
    ENDIF()
ENDFOREACH()